export interface RingSectionItem {
    [key: string]: any;
    percentage: number;
    color?: string;
}

export interface RingSemiSectionItem {
    index: number;
    rotation: number;
    color: string;
}
